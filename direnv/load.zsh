# Load direnv, an environment switcher for the shell.
# This allows to have project-specific environment variables and not clutter
# the "~/.profile" file.
#
# See https://github.com/zimbatm/direnv

eval "$(direnv hook zsh)"

